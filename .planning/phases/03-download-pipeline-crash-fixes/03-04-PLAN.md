---
phase: 03-download-pipeline-crash-fixes
plan: 04
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lapce-app/src/keypress/condition.rs
  - lapce-app/src/keypress/loader.rs
autonomous: true
requirements:
  - CRASH-01
  - TEST-01

must_haves:
  truths:
    - "check_condition('totally_unknown_condition_xyz', &focus) returns false — not panic"
    - "check_condition('!totally_unknown_condition_xyz', &focus) returns true — not panic"
    - "KeyMapLoader::load_from_str emits tracing::warn! for unparseable 'when' condition tokens"
    - "Regression tests in condition.rs mod test pass under cargo test -p lapce-app"
  artifacts:
    - path: "lapce-app/src/keypress/condition.rs"
      provides: "CRASH-01 regression tests locking unknown-condition semantics (D-11)"
    - path: "lapce-app/src/keypress/loader.rs"
      provides: "D-10 load-time warn! for unparseable condition tokens"
  key_links:
    - from: "lapce-app/src/keypress/loader.rs load_from_str"
      to: "Condition::from_str (strum EnumString)"
      via: "token.parse::<Condition>().is_err()"
      pattern: "parse::<Condition>"
---

<objective>
Lock the CRASH-01 non-panic guarantee with regression tests and add load-time diagnostic logging
for malformed keymap `when` conditions. The check_condition evaluator is already panic-free (D-09
confirms the old `unwrap()` was removed upstream); Phase 3 work is purely a regression test (TEST-01)
and a load-time `tracing::warn!` (D-10) — no behaviour change at eval time.

Depends on Plan 01 (03-01) for ordering hygiene (Wave 2), though technically the files touched here
have no runtime dependency on the async migration. The wave-2 slot keeps execution ordered.

Output: Two new test functions in `condition.rs`'s existing `#[cfg(test)] mod test` block;
updated `loader.rs` with a `warn!` call and an updated tracing import.
</objective>

<execution_context>
@/Users/picard/gitbase/lapce/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/gitbase/lapce/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/picard/gitbase/lapce/.planning/ROADMAP.md
@/Users/picard/gitbase/lapce/.planning/phases/03-download-pipeline-crash-fixes/03-CONTEXT.md
@/Users/picard/gitbase/lapce/.planning/phases/03-download-pipeline-crash-fixes/03-RESEARCH.md
@/Users/picard/gitbase/lapce/.planning/phases/03-download-pipeline-crash-fixes/03-PATTERNS.md
@/Users/picard/gitbase/lapce/.planning/codebase/CONVENTIONS.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add CRASH-01 regression tests to condition.rs and D-10 load-time warn to loader.rs</name>
  <files>
    lapce-app/src/keypress/condition.rs
    lapce-app/src/keypress/loader.rs
  </files>
  <read_first>
    - lapce-app/src/keypress/condition.rs — read the full file; locate the `#[cfg(test)] mod test` block (lines 76–162); identify MockFocus definition (lines 84–110); understand what KeyPressData::check_condition and CheckCondition::check_condition accept as arguments; confirm Condition::from_str / strum EnumString is the parse mechanism
    - lapce-app/src/keypress.rs — read lines 524–561 to see the full check_condition implementation and confirm it is panic-free (no .unwrap() on the Condition::from_str result)
    - lapce-app/src/keypress/loader.rs — read fully (esp. lines 1–50 for imports, lines 90–130 for load_from_str and KeyMap construction); confirm tracing import and the existing error! call at lines ~39–43; confirm the `when` field type on KeyMap (Option&lt;String&gt;)
    - .planning/phases/03-download-pipeline-crash-fixes/03-PATTERNS.md §condition.rs and §loader.rs — exact test shapes and warn pattern
    - .planning/phases/03-download-pipeline-crash-fixes/03-RESEARCH.md §CRASH-01
  </read_first>
  <behavior>
    - check_condition with an unknown token returns false (not panic, not Ok/Err — the function returns bool)
    - check_condition with a negated unknown token returns true
    - Regression test 1: `unknown_condition_evaluates_to_false_not_panic` passes (asserts false)
    - Regression test 2: `negated_unknown_condition_evaluates_to_true_not_panic` passes (asserts true)
    - loader.rs: when a keymap's `when` field contains a token that fails `Condition::from_str`, tracing::warn! is emitted (visible in tracing output, not a panic or silent skip)
    - No behaviour change at eval time — only load-time warning is added
  </behavior>
  <action>
    In lapce-app/src/keypress/condition.rs, append two new test functions inside the existing
    `#[cfg(test)] mod test` block (after the existing `test_check_condition` function):

    Test 1 — unknown condition evaluates to false:
    ```
    #[test]
    fn unknown_condition_evaluates_to_false_not_panic() {
        let focus = MockFocus { accepted_conditions: &[] };
        assert!(
            !KeyPressData::check_condition("totally_unknown_condition_xyz", &focus),
            "unknown condition token must evaluate to false, not panic"
        );
    }
    ```

    Test 2 — negated unknown condition evaluates to true:
    ```
    #[test]
    fn negated_unknown_condition_evaluates_to_true_not_panic() {
        let focus = MockFocus { accepted_conditions: &[] };
        assert!(
            KeyPressData::check_condition("!totally_unknown_condition_xyz", &focus),
            "negated unknown condition token must evaluate to true, not panic"
        );
    }
    ```

    Use the existing MockFocus (already defined in the test module — do NOT add a second mock struct).
    Adjust the function call signature if `check_condition` takes the focus by reference differently
    than shown — read the actual signature first (per read_first instruction).
    Per D-09, D-11; PATTERNS.md §condition.rs.

    In lapce-app/src/keypress/loader.rs `load_from_str` function:
    After the successful construction of each `KeyMap` value (the struct is built and pushed or
    returned — read the exact lines), add a validation block:
    ```
    if let Some(ref when) = keymap.when {
        for token in when
            .split(|c: char| c == '|' || c == '&')
            .map(|t| t.trim().trim_start_matches('!'))
            .filter(|t| !t.is_empty())
        {
            if token.parse::<Condition>().is_err() {
                tracing::warn!(
                    "Unparseable condition token {:?} in keymap {:?}",
                    token,
                    keymap.command
                );
            }
        }
    }
    ```
    This must import `Condition` from the condition module — check existing use declarations in loader.rs.
    If `Condition` is not yet imported, add `use super::condition::Condition;` or the appropriate path.
    Update the tracing import: if it is `use tracing::{debug, error};`, change to
    `use tracing::{debug, error, warn};`. Alternatively use the fully qualified `tracing::warn!(...)`.
    Per D-10; PATTERNS.md §loader.rs.
  </action>
  <verify>
    <automated>cargo test -p lapce-app unknown_condition negated_unknown 2>&1 | tail -15</automated>
  </verify>
  <done>
    - Two new test functions exist in condition.rs mod test: `unknown_condition_evaluates_to_false_not_panic` and `negated_unknown_condition_evaluates_to_true_not_panic`
    - Both tests pass under `cargo test -p lapce-app`
    - loader.rs contains a `tracing::warn!` call for unparseable condition tokens in `load_from_str`
    - loader.rs still imports/uses `warn` (added to existing tracing use or used fully-qualified)
    - No behaviour change at eval time — only load-time warning
    - cargo check -p lapce-app exits 0
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Keymap file (user config) → load_from_str | Keymap files are user-controlled; malformed `when` conditions are benign (produce a warning and eval to false/true) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-14 | Denial of Service | Malformed `when` condition in keymap | mitigate | Unknown tokens evaluate to false/true (harmless), load-time warn logged; no crash, no hang |
| T-03-15 | Spoofing | Keymap `when` evaluating unknown tokens permissively | accept | D-11 locked semantics: unknown = false (safe), !unknown = true (permissive). Accepted tradeoff documented in CONTEXT.md — revisit only if users report silent keymap breakage |
| T-03-SC | Tampering | No new package installs | accept | Zero new dependencies; slopcheck not applicable |
</threat_model>

<verification>
1. `grep -n "unknown_condition_evaluates_to_false_not_panic\|negated_unknown_condition" lapce-app/src/keypress/condition.rs` — must find both test functions
2. `grep -n "tracing::warn!\|warn!" lapce-app/src/keypress/loader.rs` — must find the warn call
3. `cargo test -p lapce-app unknown_condition negated_unknown` — must exit 0 with both tests passing
4. `cargo check -p lapce-app` — must exit 0
5. `grep -n "Condition::from_str\|parse::<Condition>" lapce-app/src/keypress/loader.rs` — must find the token parsing in the warn block
</verification>

<success_criteria>
- `condition.rs` test module contains `unknown_condition_evaluates_to_false_not_panic` and `negated_unknown_condition_evaluates_to_true_not_panic`; both pass
- `loader.rs` `load_from_str` emits `tracing::warn!` for any keymap `when` token that fails `Condition::from_str`
- No eval-time behaviour change — unknown tokens still silently evaluate (no per-keystroke toast)
- `cargo test -p lapce-app` exits 0 for these tests
</success_criteria>

<output>
Create `.planning/phases/03-download-pipeline-crash-fixes/03-04-SUMMARY.md` when done.
</output>
