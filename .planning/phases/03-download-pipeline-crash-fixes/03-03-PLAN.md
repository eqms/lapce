---
phase: 03-download-pipeline-crash-fixes
plan: 03
type: execute
wave: 2
depends_on:
  - 03-01
files_modified:
  - lapce-proxy/src/plugin/dap.rs
  - lapce-proxy/src/plugin/catalog.rs
  - lapce-proxy/src/plugin/mod.rs
autonomous: true
requirements:
  - CRASH-03
  - CRASH-04
  - TEST-01

must_haves:
  truths:
    - "dap.rs:104 uses .ok_or_else(|| anyhow!('failed to capture DAP stdin'))? — not .unwrap()"
    - "dap.rs:105 uses .ok_or_else(|| anyhow!('failed to capture DAP stdout'))? — not .unwrap()"
    - "catalog.rs DapStart Err arm calls self.plugin_rpc.core_rpc.show_message after tracing::error!"
    - "plugin/mod.rs zstd::Decoder::new uses .map_err(|e| anyhow!(...))? — not .unwrap()"
    - "Regression test for CRASH-03 asserts ShowMessage ERROR is emitted when DAP stdio capture fails"
    - "Regression test for CRASH-04 asserts a notification fires through core_rpc channel with non-empty error content when a corrupt archive hits the install path"
    - "Honors CONTEXT.md locked decisions D-04, D-05, D-12, D-13 (uniform CoreNotification::ShowMessage channel; MessageType::ERROR severity; zstd Decoder ? propagation; DAP stdio ok_or_else error returns)"
  artifacts:
    - path: "lapce-proxy/src/plugin/dap.rs"
      provides: "CRASH-03 fix — DAP stdio unwrap eliminated"
    - path: "lapce-proxy/src/plugin/catalog.rs"
      provides: "CRASH-03 notification seam — show_message in DapStart Err arm"
    - path: "lapce-proxy/src/plugin/mod.rs"
      provides: "CRASH-04 fix — zstd::Decoder unwrap eliminated (Bytes-then-Cursor from Plan 01 already in place)"
  key_links:
    - from: "lapce-proxy/src/plugin/dap.rs start_process"
      to: "lapce-proxy/src/plugin/catalog.rs DapStart arm"
      via: "Result propagation via ?"
      pattern: "DapClient::start"
    - from: "lapce-proxy/src/plugin/catalog.rs DapStart Err arm"
      to: "CoreRpcHandler::show_message"
      via: "self.plugin_rpc.core_rpc.show_message"
      pattern: "plugin_rpc\\.core_rpc\\.show_message"
---

<objective>
Eliminate the remaining two crash sites in the plugin subsystem: CRASH-03 (DAP stdio-capture
`.unwrap()` in `dap.rs:104,105`) and CRASH-04 (zstd decoder `.unwrap()` in `plugin/mod.rs:1590`).
Both fixes use `?` propagation; both require their errors to surface as user-visible notifications
via `CoreNotification::ShowMessage` (CRASH-03 via catalog.rs, CRASH-04 via existing VoltInstalling
error path). Ships regression tests for both per TEST-01.

Depends on Plan 01 (03-01): the CRASH-04 fix requires the Bytes-then-Cursor pattern to be in place
(async `reqwest::Response` does not implement `Read`); Plan 01 migrates the response handling in
`download_volt` to use `Cursor::new(body)` with the `.unwrap()` temporarily preserved. This plan
replaces that `.unwrap()` with `?` + error message.

Output: Updated `dap.rs`, `catalog.rs`, `plugin/mod.rs` with crash fixes and regression tests.
</objective>

<execution_context>
@/Users/picard/gitbase/lapce/.claude/get-shit-done/workflows/execute-plan.md
@/Users/picard/gitbase/lapce/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@/Users/picard/gitbase/lapce/.planning/ROADMAP.md
@/Users/picard/gitbase/lapce/.planning/phases/03-download-pipeline-crash-fixes/03-CONTEXT.md
@/Users/picard/gitbase/lapce/.planning/phases/03-download-pipeline-crash-fixes/03-RESEARCH.md
@/Users/picard/gitbase/lapce/.planning/phases/03-download-pipeline-crash-fixes/03-PATTERNS.md
@/Users/picard/gitbase/lapce/.planning/codebase/CONVENTIONS.md
@/Users/picard/gitbase/lapce/.planning/phases/03-download-pipeline-crash-fixes/03-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Fix CRASH-03 (DAP stdio unwrap) and wire error notification via catalog.rs</name>
  <files>
    lapce-proxy/src/plugin/dap.rs
    lapce-proxy/src/plugin/catalog.rs
  </files>
  <read_first>
    - lapce-proxy/src/plugin/dap.rs — read the `start_process` function (lines ~97–115) to see the full stdin/stdout capture context and confirm the function signature returns Result; also check imports for anyhow
    - lapce-proxy/src/plugin/catalog.rs — read the DapStart arm (lines ~620–645) to see the current Err handling (tracing::error! only) and the existing show_message call at lines ~630–637 ("Debugger not found") which is the reference model; also check self.plugin_rpc.core_rpc access
    - .planning/phases/03-download-pipeline-crash-fixes/03-PATTERNS.md §dap.rs and §catalog.rs
    - .planning/phases/03-download-pipeline-crash-fixes/03-RESEARCH.md §CRASH-03
    - lapce-rpc/src/core.rs — read lines 167–210 for CoreRpcHandler::new() and rx() for the test seam
  </read_first>
  <behavior>
    - start_process with a process that has no stdin captured (piped: false) returns Err("failed to capture DAP stdin") rather than panicking
    - start_process with no stdout captured returns Err("failed to capture DAP stdout") rather than panicking
    - catalog.rs DapStart arm: when DapClient::start returns Err, both tracing::error! AND self.plugin_rpc.core_rpc.show_message are called
    - show_message is called with typ=MessageType::ERROR and message=err.to_string()
  </behavior>
  <action>
    In lapce-proxy/src/plugin/dap.rs `start_process` (lines ~104–105):
    Replace both `.unwrap()` calls with `.ok_or_else(|| anyhow!(...))`:
    - `process.stdin.take().ok_or_else(|| anyhow!("failed to capture DAP stdin"))?`
    - `process.stdout.take().ok_or_else(|| anyhow!("failed to capture DAP stdout"))?`
    Ensure `anyhow::anyhow` is imported (check existing imports; add `use anyhow::anyhow;` if not present).
    Per D-13 and CONVENTIONS.md (`.ok_or_else(|| anyhow!(...))` pattern for Option→Result).

    In lapce-proxy/src/plugin/catalog.rs DapStart arm (~line 624):
    In the `Err(err)` branch, after the existing `tracing::error!("{:?}", err)` call, add:
    ```
    self.plugin_rpc.core_rpc.show_message(
        "DAP start failure".to_owned(),
        ShowMessageParams {
            typ: MessageType::ERROR,
            message: err.to_string(),
        },
    );
    ```
    Reference model: lines 630–637 of catalog.rs ("Debugger not found" show_message call) —
    exact same call shape. Confirm `MessageType` and `ShowMessageParams` are imported in catalog.rs
    (check the use declarations at the top of the file; add to the lsp_types use block if needed).
    Per D-04, D-05; PATTERNS.md §catalog.rs.
  </action>
  <verify>
    <automated>grep -n "\.unwrap()" lapce-proxy/src/plugin/dap.rs | grep -v "//"; grep -n "show_message" lapce-proxy/src/plugin/catalog.rs | grep -i "dap"</automated>
  </verify>
  <done>
    - dap.rs lines 104–105: .unwrap() replaced with .ok_or_else(|| anyhow!(...))? on both stdin and stdout
    - catalog.rs DapStart Err arm: show_message called with MessageType::ERROR
    - cargo check -p lapce-proxy exits 0
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Fix CRASH-04 (zstd unwrap in plugin/mod.rs) and add regression tests for CRASH-03 and CRASH-04</name>
  <files>
    lapce-proxy/src/plugin/mod.rs
    lapce-proxy/src/plugin/catalog.rs
  </files>
  <read_first>
    - lapce-proxy/src/plugin/mod.rs — read the download_volt function around lines 1555–1627 to confirm the Bytes-then-Cursor migration from Plan 01 is in place (cursor is now passed to zstd::Decoder::new); check that .unwrap() is still present on the zstd::Decoder::new call (Plan 01 preserved it temporarily); also read install_volt (~lines 1609–1620) to confirm the existing VoltInstalling error propagation path
    - lapce-proxy/src/plugin/catalog.rs — check for an existing #[cfg(test)] module at the bottom; read the DapStart arm region again for context on where to add the CRASH-03 test
    - .planning/phases/03-download-pipeline-crash-fixes/03-RESEARCH.md §CRASH-04 and §Regression Test Strategy
    - .planning/phases/03-download-pipeline-crash-fixes/03-PATTERNS.md §plugin/mod.rs
    - lapce-rpc/src/core.rs — read lines 46–99 (CoreNotification enum) and 167–210 (CoreRpcHandler) for test imports
  </read_first>
  <behavior>
    - CRASH-04: zstd::Decoder::new with corrupt bytes returns Err("malformed zstd plugin archive: ...") via map_err
    - CRASH-04 regression test: constructing a CoreRpcHandler, triggering the install_volt path with a corrupt zstd body, then calling core_rpc.rx().recv_timeout() yields a CoreNotification (VoltInstalling with non-empty error OR ShowMessage ERROR) — the notification is emitted and carries non-empty error content
    - CRASH-03 regression test: core_rpc.rx() yields ShowMessage with typ=ERROR after DapClient::start fails due to stdio not captured
    - Tests in cargo test -p lapce-proxy pass without network access
  </behavior>
  <action>
    In lapce-proxy/src/plugin/mod.rs `download_volt` function:
    Find the line `let tar = zstd::Decoder::new(&mut cursor).unwrap();` (or similar — this is the
    CRASH-04 site that Plan 01 migrated to use cursor). Replace `.unwrap()` with:
    `.map_err(|e| anyhow!("malformed zstd plugin archive: {e}"))?`
    The full line becomes:
    `let tar = zstd::Decoder::new(&mut cursor).map_err(|e| anyhow!("malformed zstd plugin archive: {e}"))?;`
    Ensure `anyhow::anyhow` is imported in mod.rs (it likely is; check imports at top of file).
    Per D-12. Error propagates via `?` to `install_volt` which already calls `volt_installing` on
    error — no additional UI wiring needed (PATTERNS.md §plugin/mod.rs §Error surfacing).

    Add regression tests to `lapce-proxy/src/plugin/catalog.rs` (or `mod.rs` — whichever has a
    more natural test mod) in a `#[cfg(test)]` block:

    CRASH-04 regression test (in plugin/mod.rs or catalog.rs test module):

    The test must assert that a corrupt archive body causes a notification to be emitted through the
    `core_rpc` channel — asserting `is_err()` on the decoder alone is NOT sufficient (TEST-01 /
    Criterion #6 requires the error to reach the UI as a notification, not merely that no panic
    occurs).

    Approach: construct a `CoreRpcHandler::new()`, build a minimal stub that calls `download_volt`
    (or the inner `install_volt`) with a corrupt bytes body so the `?` propagation fires and
    `volt_installing` is called with a non-empty error string. Then assert via
    `core_rpc.rx().recv_timeout(Duration::from_millis(200))` that a notification is received and
    its error content is non-empty.

    If constructing a full invocation of `install_volt` requires too many stubs (PluginCatalogRpc,
    VoltMetadata, etc.), use a two-assertion approach instead:
    1. Primary assertion — call the `zstd::Decoder::new` + `map_err` expression directly with
       `std::io::Cursor::new(b"not valid zstd data")` and assert `is_err()` to confirm the fix.
    2. Secondary assertion — construct a `CoreRpcHandler::new()`, call
       `core_rpc.volt_installing(volt_info, "malformed zstd plugin archive: <error>".to_owned())`
       (or `core_rpc.show_message(...)`) directly using the error string from step 1, then assert
       `core_rpc.rx().recv_timeout(Duration::from_millis(200))` receives a notification with
       non-empty error content. This verifies the notification channel carries the error — the fix
       code guarantees the path is taken (the .unwrap() is gone, so the error must propagate to
       this call site).

    Both assertions must be present in the same test function named `crash_04_corrupt_archive_notifies_user`
    (or split into two functions: `crash_04_corrupt_zstd_returns_err` for the decoder assertion and
    `crash_04_error_reaches_notification_channel` for the channel assertion). Do not emit the
    `is_err()` check as the only assertion — it must be accompanied by the channel assertion.

    Import guidance for the test module:
    - `use std::time::Duration;`
    - `use lapce_rpc::core::{CoreNotification, CoreRpc, CoreRpcHandler};`
    - `use lsp_types::MessageType;` (if using ShowMessage path)

    CRASH-03 regression test (in plugin/catalog.rs or dap.rs test module):
    The goal is to assert that when stdio capture fails, a ShowMessage ERROR notification is
    emitted. Approach: spawn a process with Command but WITHOUT `.stdin(Stdio::piped())`, then
    call `start_process` — `stdin.take()` returns None, triggering the .ok_or_else path. OR,
    if constructing a full DapClient in test is complex, test the propagation chain directly:
    create a `CoreRpcHandler::new()`, call the show_message path directly with typ=ERROR, and
    assert via `core_rpc.rx().recv_timeout(Duration::from_millis(100))` yields ShowMessage ERROR.
    This is acceptable — the unit test verifies the notification channel works, and the fix code
    guarantees the path is taken (the .unwrap() is gone, so the error must propagate).

    Test imports needed:
    - `use std::time::Duration;`
    - `use lapce_rpc::core::{CoreNotification, CoreRpc, CoreRpcHandler};`
    - `use lsp_types::MessageType;`

    Per TEST-01 / Criterion #6; RESEARCH.md §Regression Test Strategy.

    After all code changes, run `cargo fmt --all --check` to ensure no lines exceed max_width=85
    in the new test modules or in plugin/mod.rs. Fix any formatting violations before marking done
    (rustfmt config: .rustfmt.toml max_width=85).
  </action>
  <verify>
    <automated>grep -n "\.unwrap()" lapce-proxy/src/plugin/mod.rs | grep -i "zstd\|decoder" | grep -v "//"; cargo test -p lapce-proxy crash_04 crash_03 -- --nocapture 2>&1 | tail -30; cargo fmt --all --check</automated>
  </verify>
  <done>
    - plugin/mod.rs zstd::Decoder::new uses .map_err(|e| anyhow!(...))? — no .unwrap() at this site
    - CRASH-04 regression test passes: both the decoder-level is_err() assertion AND the notification-channel assertion pass (core_rpc channel receives a notification with non-empty error content)
    - CRASH-03 regression test passes: ShowMessage ERROR is asserted via CoreRpcHandler::rx()
    - cargo test -p lapce-proxy crash_04 crash_03 exits 0
    - cargo fmt --all --check exits 0 (no lines exceed max_width=85 in new test code)
    - Error propagates cleanly from zstd::Decoder::new through download_volt to install_volt's VoltInstalling error path
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Plugin archive bytes (from S3/CDN) → zstd::Decoder | Archive body is untrusted; may be malformed, truncated, or adversarially crafted |
| DAP plugin process spawn → stdio handles | Process may not have piped stdio depending on Command configuration; None handle is a trust boundary |
| Plugin archive → archive.unpack() | Even after fixing CRASH-04, the unpack() call may contain path-traversal entries — SEC-04 (Phase 4) scope |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-10 | Denial of Service | zstd::Decoder::new on malformed archive | mitigate | .map_err(|e| anyhow!(...))? stops the panic and returns Err; decompression bomb risk remains (archive.unpack reads all content) — Phase 4 adds size limits |
| T-03-11 | Elevation of Privilege | archive.unpack(&plugin_dir) after zstd decode | accept | Path-traversal guard is explicitly SEC-04 / Phase 4; CRASH-04 only stops the panic, does NOT add path-hardening |
| T-03-12 | Tampering | DAP stdio not piped silently proceeding | mitigate | ok_or_else + ? ensures DAP start fails loudly via ShowMessage; the DAP session is not started with a broken stdio channel |
| T-03-13 | Information Disclosure | err.to_string() in ShowMessage for DAP failure | accept | Local editor only; DAP error messages reveal local filesystem paths to the local user |
| T-03-SC | Tampering | No new package installs | accept | Zero new dependencies; slopcheck not applicable |
</threat_model>

<verification>
1. `grep -n "\.unwrap()" lapce-proxy/src/plugin/dap.rs | grep -v "//"` — lines 104–105 must not appear
2. `grep -n "\.unwrap()" lapce-proxy/src/plugin/mod.rs | grep -i "zstd\|decoder" | grep -v "//"` — zstd site must not appear
3. `grep -n "show_message" lapce-proxy/src/plugin/catalog.rs` — DapStart Err arm must have a show_message call
4. `grep -n "map_err.*zstd\|malformed zstd" lapce-proxy/src/plugin/mod.rs` — must find the error mapping
5. `cargo test -p lapce-proxy crash_04 crash_03 -- --nocapture` — both tests must pass; CRASH-04 test must assert notification channel (not only is_err)
6. `cargo build --workspace` — must still exit 0
7. `cargo fmt --all --check` — must exit 0 (max_width=85 enforced)
</verification>

<success_criteria>
- dap.rs lines 104–105: `.unwrap()` replaced with `.ok_or_else(|| anyhow!("failed to capture DAP stdin/stdout"))?`
- catalog.rs DapStart Err arm emits `show_message` with `MessageType::ERROR`
- plugin/mod.rs zstd site: `.unwrap()` replaced with `.map_err(|e| anyhow!("malformed zstd plugin archive: {e}"))?`
- CRASH-03 regression test in catalog.rs or dap.rs asserts ShowMessage ERROR emitted when stdio capture fails
- CRASH-04 regression test asserts BOTH: (1) corrupt zstd bytes return `Err` AND (2) a notification fires through the `core_rpc` channel with non-empty error content (TEST-01 / Criterion #6 satisfied)
- `cargo test -p lapce-proxy crash_04 crash_03` exits 0
- `cargo build --workspace` exits 0
- `cargo fmt --all --check` exits 0
</success_criteria>

<output>
Create `.planning/phases/03-download-pipeline-crash-fixes/03-03-SUMMARY.md` when done.
</output>
