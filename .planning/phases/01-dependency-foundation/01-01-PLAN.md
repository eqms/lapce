---
phase: 01-dependency-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - Cargo.toml
  - lapce-app/Cargo.toml
autonomous: true
requirements:
  - DEPS-01
  - DEPS-02
  - DEPS-03
  - DEPS-05
  - DEPS-06
  - DEPS-07

must_haves:
  truths:
    - "cargo build --workspace exits 0 after all Cargo.toml version pins are applied"
    - "cargo tree -i zip shows only zip 2.x in the dependency tree"
    - "cargo tree -i reqwest shows reqwest 0.12.x"
    - "No git-rev source lines remain for tracing, alacritty_terminal, or floem in Cargo.toml"
    - "tokio is present as an explicit workspace dependency at version 1.52.x"
    - "sha2 is declared in [workspace.dependencies] and referenced via workspace = true in lapce-app"
    - "toml version is pinned to 0.8 (not wildcard)"
  artifacts:
    - path: "Cargo.toml"
      provides: "Updated workspace dependency pins"
      contains: "reqwest.*0.12.28"
    - path: "Cargo.toml"
      provides: "tokio workspace dep"
      contains: "tokio.*1.52.3"
    - path: "lapce-app/Cargo.toml"
      provides: "zip 2.4.0 + sha2 workspace ref"
      contains: "zip.*2.4.0"
  key_links:
    - from: "Cargo.toml [workspace.dependencies]"
      to: "lapce-app/Cargo.toml"
      via: "workspace = true"
      pattern: "sha2.*workspace.*true"
    - from: "Cargo.toml [workspace.dependencies.floem]"
      to: "lapce-app rfd file dialogs"
      via: "features rfd-tokio"
      pattern: "rfd-tokio"
---

<objective>
Apply all Cargo.toml-only dependency version pins for Phase 1.

Purpose: Establish the dependency foundation that Phase 2 and beyond build on. This
plan makes zero code changes — it only edits Cargo.toml files. All seven DEPS
requirements involving only version numbers are landed here; the code-site
migrations that fix resulting compile errors are in plan 01-02.

Output: Updated Cargo.toml (workspace) and lapce-app/Cargo.toml with:
- reqwest 0.11 → 0.12.28 (DEPS-01)
- tokio 1.52.3 added as explicit workspace dep (DEPS-02)
- zip 0.6.6 → 2.4.0 CVE fix (DEPS-03)
- toml wildcard → "0.8" (DEPS-05)
- tracing/tracing-subscriber/tracing-appender/tracing-log: git rev → stable crates.io (DEPS-06)
- alacritty_terminal: git rev → crates.io 0.24.1 (DEPS-06)
- floem: git rev → crates.io 0.2.0 with rfd-tokio feature (DEPS-06)
- sha2 promoted to workspace dep (DEPS-07)

IMPORTANT: After this plan, the workspace will NOT compile — plan 01-02 fixes the
code-site compile errors introduced by the tracing and interprocess upgrades. This
is expected and correct.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/01-dependency-foundation/01-RESEARCH.md
@.planning/phases/01-dependency-foundation/01-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Workspace Cargo.toml — all version pin changes</name>
  <files>Cargo.toml</files>
  <read_first>
    Read Cargo.toml lines 33–124 (the entire [workspace.dependencies] section plus the
    four tracing stanzas and the alacritty/floem stanzas) before making any edits.
    Read .planning/phases/01-dependency-foundation/01-RESEARCH.md sections DEPS-01
    through DEPS-07 and the "Recommended Cargo.toml Change Summary" block.
    Read .planning/phases/01-dependency-foundation/01-PATTERNS.md "Cargo.toml
    (workspace root, config)" section for the exact before/after values.
  </read_first>
  <action>
    Apply the following changes to Cargo.toml in a single edit. Do not change any
    lines not listed below. Do not change [patch.crates-io] entries.

    1. Line 47 — DEPS-04 version bump (interprocess stays here; code rewrite is
       plan 01-02 task 1):
       Change: interprocess = { version = "1.2.1" }
       To:     interprocess = { version = "2.4.2" }

    2. Line 56 — DEPS-01:
       Change: reqwest = { version = "0.11", features = ["blocking", "json", "socks"] }
       To:     reqwest = { version = "0.12.28", features = ["blocking", "json", "socks"] }

    3. After line 56 — DEPS-02: insert new workspace dep:
       tokio = { version = "1.52.3", features = ["rt-multi-thread", "macros", "sync", "time", "fs"] }

    4. After new tokio line — DEPS-07: insert new workspace dep:
       sha2 = { version = "0.10.8" }

    5. Line 65 (toml) — DEPS-05:
       Change: toml = { version = "*" }
       To:     toml = { version = "0.8" }

    6. Lines 79–83 — DEPS-06 floem: replace the entire [workspace.dependencies.floem]
       stanza (including the git/rev lines) with:
         [workspace.dependencies.floem]
         version  = "0.2.0"
         features = ["editor", "serde", "default-image-formats", "rfd-tokio"]

       Also update [workspace.dependencies.floem-editor-core] (lines 85–89):
         [workspace.dependencies.floem-editor-core]
         version  = "0.2.0"
         features = ["serde"]

       FALLBACK INSTRUCTION (execute if `cargo build --workspace` fails with floem
       compile errors after plan 01-02 completes): revert floem to:
         [workspace.dependencies.floem]
         git      = "https://github.com/lapce/floem"
         rev      = "31fa8f444c37f4c314f47d88c23ffdbc25f2ab53"
         features = ["editor", "serde", "default-image-formats", "rfd-tokio"]
         [workspace.dependencies.floem-editor-core]
         git      = "https://github.com/lapce/floem"
         rev      = "31fa8f444c37f4c314f47d88c23ffdbc25f2ab53"
         features = ["serde"]
       The rfd-tokio feature must be present in both paths (no fallback to rfd-async-std).

    7. Lines 101–119 — DEPS-06 tracing family: replace all four [workspace.dependencies.tracing*]
       stanzas (each currently has git/rev/package keys) with:
         [workspace.dependencies.tracing]
         version = "0.1.44"

         [workspace.dependencies.tracing-log]
         version = "0.2.0"

         [workspace.dependencies.tracing-subscriber]
         version  = "0.3.23"
         features = ["fmt", "env-filter", "registry"]

         [workspace.dependencies.tracing-appender]
         version = "0.2.5"

    8. Lines 121–123 — DEPS-06 alacritty_terminal: replace git/rev stanza with:
         [workspace.dependencies.alacritty_terminal]
         version = "0.24.1"

       FALLBACK INSTRUCTION: if `cargo build --workspace` fails with alacritty_terminal
       compile errors, revert to:
         [workspace.dependencies.alacritty_terminal]
         git = "https://github.com/alacritty/alacritty"
         rev = "cacdb5bb3b72bad2c729227537979d95af75978f"

    CRITICAL: reqwest features ["blocking", "json", "socks"] must be retained exactly.
    The `blocking` feature is NOT default in reqwest 0.12 — dropping it breaks
    lapce-proxy/src/lib.rs at compile time.
  </action>
  <verify>
    <automated>
      cargo metadata --no-deps --format-version 1 | grep -o '"reqwest":"[^"]*"' | head -5
    </automated>
  </verify>
  <acceptance_criteria>
    - Cargo.toml contains: reqwest = { version = "0.12.28", features = ["blocking", "json", "socks"] }
    - Cargo.toml contains: tokio = { version = "1.52.3"
    - Cargo.toml contains: sha2 = { version = "0.10.8" }
    - Cargo.toml contains: toml = { version = "0.8" }
    - Cargo.toml contains: interprocess = { version = "2.4.2" }
    - Cargo.toml does NOT contain: git = "https://github.com/tokio-rs/tracing"
    - Cargo.toml does NOT contain: git = "https://github.com/alacritty/alacritty" (unless fallback was triggered)
    - Cargo.toml [workspace.dependencies.floem] contains: rfd-tokio (not rfd-async-std)
    - Cargo.toml [workspace.dependencies.tracing] contains: version = "0.1.44"
    - Cargo.toml [workspace.dependencies.tracing-subscriber] contains: version = "0.3.23"
  </acceptance_criteria>
  <done>All workspace dependency version pins updated; no git-rev lines remain for tracing or alacritty; floem uses rfd-tokio; tokio and sha2 are new workspace entries.</done>
</task>

<task type="auto">
  <name>Task 2: lapce-app/Cargo.toml — zip upgrade and sha2 workspace reference</name>
  <files>lapce-app/Cargo.toml</files>
  <read_first>
    Read lapce-app/Cargo.toml in full (84 lines) before editing.
    Read .planning/phases/01-dependency-foundation/01-PATTERNS.md
    "lapce-app/Cargo.toml (config)" section for exact before/after values.
    Read .planning/phases/01-dependency-foundation/01-RESEARCH.md DEPS-03 and DEPS-07
    sections to understand the zip feature flag and sha2 promotion.
  </read_first>
  <action>
    Apply two changes to lapce-app/Cargo.toml:

    1. Line 62 — DEPS-07 sha2 promotion:
       Change: sha2 = { version = "0.10.8" }
       To:     sha2 = { workspace = true }

    2. Line 63 — DEPS-03 zip CVE fix:
       Change: zip = { version = "0.6.6", default-features = false, features = ["deflate"] }
       To:     zip = { version = "2.4.0", default-features = false, features = ["deflate"] }

    No other lines change. The zip feature "deflate" is valid in 2.4.0.
    Do not add tokio = { workspace = true } here — it is not needed in lapce-app in Phase 1.
  </action>
  <verify>
    <automated>
      grep -n "sha2\|zip" /Users/picard/gitbase/lapce/lapce-app/Cargo.toml
    </automated>
  </verify>
  <acceptance_criteria>
    - lapce-app/Cargo.toml line for sha2 reads: sha2 = { workspace = true }
    - lapce-app/Cargo.toml line for zip reads: zip = { version = "2.4.0", default-features = false, features = ["deflate"] }
    - No other lines in lapce-app/Cargo.toml changed
  </acceptance_criteria>
  <done>lapce-app declares sha2 from workspace and zip at 2.4.0; the two-line edit is complete.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo registry → workspace | Crate versions resolved from crates.io; yanked versions skipped by Cargo |
| Workspace → lapce-app | lapce-app/Cargo.toml inherits workspace dep versions |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-01 | Tampering | zip 0.6.6 → 2.4.0 (CVE-2025-29787) | mitigate | zip 2.4.0 built-in `enclosed_name` validation rejects `../` traversal entries in `ZipArchive::extract`; regression test in plan 01-02 asserts fail-closed behavior |
| T-01-02 | Tampering | floem git rev → crates.io | accept | floem is a UI framework, not a security boundary; if crates.io 0.2.0 diverges from git rev, fallback instruction restores the git pin |
| T-01-03 | Tampering | tracing git rev → crates.io stable | accept | tracing is a logging library; no security boundary; moving to stable crates.io reduces supply-chain risk vs. floating git HEAD |
| T-01-04 | Spoofing | npm/pip/cargo installs | mitigate | Package Legitimacy Audit complete in RESEARCH.md — all packages approved; no [ASSUMED] or [SUS] packages in this plan |
| T-01-SC | Tampering | reqwest 0.12 / interprocess 2.x version bump | mitigate | versions pinned exactly (0.12.28, 2.4.2); Cargo.lock committed; slopcheck confirmed OK in RESEARCH.md |
</threat_model>

<verification>
After Task 1 and Task 2 complete:

```bash
# Verify reqwest pin
grep 'reqwest' Cargo.toml | grep '0.12.28'

# Verify tokio workspace dep exists
grep 'tokio.*1.52.3' Cargo.toml

# Verify zip CVE fix
grep 'zip.*2.4.0' lapce-app/Cargo.toml

# Verify sha2 workspace promotion
grep 'sha2.*workspace.*true' lapce-app/Cargo.toml

# Verify no git-rev tracing lines remain
grep -c 'tokio-rs/tracing' Cargo.toml  # must be 0

# Verify rfd-tokio (not rfd-async-std)
grep 'rfd-tokio' Cargo.toml

# Verify toml pinned
grep 'toml.*0\.8' Cargo.toml

# cargo check will fail (expected — code-site fixes are in plan 01-02)
# but metadata resolution should succeed:
cargo metadata --no-deps --format-version 1 > /dev/null
```

NOTE: `cargo build --workspace` is expected to FAIL after this plan because:
1. tracing-subscriber stable 0.3.23 renames `reload::Subscriber` → `reload::Layer`
   (fixed in plan 01-02 Task 1)
2. interprocess 2.x removes `LocalSocketListener` / `LocalSocketStream`
   (fixed in plan 01-02 Task 2)
Plan 01-02 must run before the workspace compiles cleanly.
</verification>

<success_criteria>
- Cargo.toml reqwest pin: "0.12.28" with features ["blocking","json","socks"]
- Cargo.toml tokio: "1.52.3" with features ["rt-multi-thread","macros","sync","time","fs"]
- Cargo.toml sha2 = { version = "0.10.8" } present in [workspace.dependencies]
- Cargo.toml toml = { version = "0.8" } (not "*")
- Cargo.toml interprocess = { version = "2.4.2" }
- Cargo.toml [workspace.dependencies.floem] has rfd-tokio, not rfd-async-std
- Cargo.toml has NO git lines for tracing family
- lapce-app/Cargo.toml sha2 = { workspace = true }
- lapce-app/Cargo.toml zip = { version = "2.4.0", ... features = ["deflate"] }
</success_criteria>

<output>
Create `.planning/phases/01-dependency-foundation/01-01-SUMMARY.md` when done.
</output>
