---
phase: 02-async-runtime-introduction
plan: 02
type: execute
wave: 2
depends_on:
  - "02-01"
files_modified:
  - lapce-app/src/runtime_tests.rs
autonomous: true
requirements:
  - RT-01

must_haves:
  truths:
    - "A regression test asserts Handle::try_current() succeeds inside an entered multi-thread runtime context"
    - "The test asserts RuntimeFlavor::MultiThread (not current_thread)"
    - "The test module is reachable by cargo test"
    - "The test guards the ambient-runtime invariant introduced by Phase 2"
  artifacts:
    - path: "lapce-app/src/runtime_tests.rs"
      provides: "Regression test for RT-01 ambient runtime invariant"
      contains: "Handle::try_current"
  key_links:
    - from: "lapce-app/src/runtime_tests.rs"
      to: "tokio::runtime::Handle::try_current()"
      via: "#[test] inside entered runtime context"
      pattern: "Handle::try_current"
---

<objective>
Ship the regression test that guards the ambient-runtime invariant (per project
Key Decision: every fix ships a regression test).

Purpose: If a future change accidentally removes the rt.enter() guard or replaces
it with a wrong binding (`let _ = rt.enter()`), this test will catch it. The test
is self-contained: it constructs its own runtime, enters it, and asserts
Handle::try_current() succeeds with MultiThread flavor.

Output:
- lapce-app/src/runtime_tests.rs — #[cfg(test)] module with one test
- Hooked into the lapce-app crate's test suite via `mod runtime_tests;` in lib.rs or app.rs
- `cargo test -p lapce-app runtime_tests` passes
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/ROADMAP.md
@.planning/phases/02-async-runtime-introduction/02-CONTEXT.md
@.planning/phases/02-async-runtime-introduction/02-RESEARCH.md
@.planning/phases/02-async-runtime-introduction/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Regression test — Handle::try_current() succeeds in entered multi-thread context</name>
  <files>lapce-app/src/runtime_tests.rs</files>

  <read_first>
    - lapce-app/src/app.rs — first 30 lines to find the module list and where to add `mod runtime_tests;`
    - lapce-app/src/lib.rs — check if this file exists; if so, add `#[cfg(test)] mod runtime_tests;` there instead of app.rs
    - .planning/phases/02-async-runtime-introduction/02-RESEARCH.md — §Code Examples (Regression Test: Handle Available)
    - .planning/phases/02-async-runtime-introduction/02-CONTEXT.md — D-03, D-05 (multi_thread, default workers)
  </read_first>

  <behavior>
    - Test: constructing a Builder::new_multi_thread().enable_all().thread_name("test-worker").build() succeeds (Ok variant)
    - Test: calling rt.enter() produces an EnterGuard without panic
    - Test: inside the entered context, Handle::try_current() returns Ok (not Err)
    - Test: the handle's runtime_flavor() equals RuntimeFlavor::MultiThread (not CurrentThread)
  </behavior>

  <action>
    Create the file lapce-app/src/runtime_tests.rs containing a single `#[cfg(test)]`
    module named `runtime_tests` with one test function
    `handle_current_succeeds_inside_entered_context`.

    The test:
    1. Calls `tokio::runtime::Builder::new_multi_thread().enable_all().thread_name("test-worker").build()` — same Builder pattern as the production entry point; use `.expect("test runtime")` here (test code, programmer-error context, `.expect()` is appropriate per project conventions).
    2. Calls `let _guard = rt.enter()` (named binding, not `let _ =`).
    3. Calls `tokio::runtime::Handle::try_current().expect("handle must be present inside entered context")`.
    4. Asserts `handle.runtime_flavor() == tokio::runtime::RuntimeFlavor::MultiThread` with `assert_eq!`.

    After creating the file, find the correct module declaration location:
    - If lapce-app/src/lib.rs exists: add `#[cfg(test)] mod runtime_tests;` there.
    - Otherwise: find the root file that declares all other modules in lapce-app/src/
      (likely app.rs or a top-level mod.rs) and add `#[cfg(test)] mod runtime_tests;` at
      the end of the module list, keeping alphabetical order if the list is sorted.

    Do NOT inline the test in app.rs or any existing file — the test lives in its own file.
    `max_width = 85`; English comments only.
  </action>

  <verify>
    <automated>cd /Users/picard/gitbase/lapce && cargo test -p lapce-app runtime_tests 2>&amp;1 | tail -15</automated>
  </verify>

  <done>
    `cargo test -p lapce-app runtime_tests` exits 0 with output containing
    "test runtime_tests::handle_current_succeeds_inside_entered_context ... ok".
    `grep "Handle::try_current" lapce-app/src/runtime_tests.rs` matches.
    `grep "RuntimeFlavor::MultiThread" lapce-app/src/runtime_tests.rs` matches.
    `cargo fmt --all --check` exits 0.
    `cargo clippy --profile ci -p lapce-app 2>&amp;1 | grep "^error"` returns empty.
    Module registered: `grep -E "mod runtime_tests" lapce-app/src/lib.rs lapce-app/src/app.rs`
    returns at least one match — proves the module is actually wired into the crate,
    not just an orphan file that cargo test silently skips.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test code | Only compiled and run under #[cfg(test)]; not present in production binary |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-02-03 | Tampering | Test runtime construction | accept | Test creates its own runtime scoped to the test function; no interaction with any production state. Torn down on test exit. |
| T-02-SC | Tampering | npm/pip/cargo installs | accept | No new packages. tokio already a workspace dep from Plan 01 in this phase. |

**Threat surface assessment:** A `#[cfg(test)]` module that constructs an in-process runtime has zero production attack surface. All existing ASVS L1 threat model notes from Plan 01 apply; no new boundaries introduced here.
</threat_model>

<verification>
After task complete:

1. `cargo test -p lapce-app runtime_tests` exits 0 — test passes
2. `grep "Handle::try_current" lapce-app/src/runtime_tests.rs` — matches
3. `grep "RuntimeFlavor::MultiThread" lapce-app/src/runtime_tests.rs` — matches
4. `grep -c "tokio::main" lapce-app/src/runtime_tests.rs` — outputs 0
5. `grep -E "mod runtime_tests" lapce-app/src/lib.rs lapce-app/src/app.rs` — at least one match
6. `cargo build --workspace` still exits 0 — no regressions from module addition
7. `cargo clippy --profile ci 2>&1 | grep "^error"` — empty
</verification>

<success_criteria>
Phase 02 plan 02 is complete when:
- lapce-app/src/runtime_tests.rs exists and is reachable by cargo test
- The module is registered via `mod runtime_tests;` in lib.rs or app.rs (verified by grep)
- The test asserts Handle::try_current() succeeds with MultiThread flavor inside an entered context
- `cargo test -p lapce-app runtime_tests` exits 0
- `cargo build --workspace` still passes
- `cargo clippy --profile ci` still clean
</success_criteria>

<output>
Create `.planning/phases/02-async-runtime-introduction/02-02-SUMMARY.md` when done.
</output>
